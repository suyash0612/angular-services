import { Injectable } from "@angular/core"

@Injectable() // not necessary but good to have.
export class SubscribeService{
    onSubscribeClicked(){
        //add user to db
    
        //send email with sub email
    
        //allow user to access the service
    
        alert('Thankyou for subscribing. You can access services now.')
      }
}